pydantic>=2.0
beautifulsoup4>=4.12
streamlit>=1.35
openai>=1.30      # used for the Azure AI Foundry path (OpenAI-wire-compatible)
anthropic>=0.30   # only needed if you use the Anthropic fallback instead
fastapi
uvicorn
pytest
httpx
