pydantic>=2.0
beautifulsoup4>=4.12
streamlit>=1.35
openai>=1.55      # AzureOpenAI client + reasoning-model params (max_completion_tokens, reasoning_effort)
langgraph>=1.0    # LangGraph orchestration (pipeline_langgraph.py, the version app.py actually runs on)
python-dotenv>=1.0  # .env loading in llm_client.py
fastapi>=0.110    # REST API endpoint (scripts/api.py)
uvicorn>=0.29     # ASGI server for the FastAPI endpoint
